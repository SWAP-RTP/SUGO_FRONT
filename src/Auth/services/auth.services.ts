// import type { Permisos } from '../types/auth.types';


// // Aquí centralizamos tus permisos simulados (mock)
// export const mockPermisos: Permisos[] = [
//     {
//         crear: false,
//         consultar: false,
//         editar: false,
//         eliminar: false
//     }
// ];

// // Simulamos una función que en el futuro hará fetch a tu API para traer estos permisos
// export const obtenerPermisosSimulados = async (): Promise<Permisos[]> => {
//     return new Promise((resolve) => {
//         setTimeout(() => {
//             resolve(mockPermisos);
//         }, 500); // Simulamos un pequeño retraso de red
//     });
// };
